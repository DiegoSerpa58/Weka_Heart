spring.application.name=weka_heart

# Disable the interactive console menu so the web server starts cleanly.
# Set to true to re-enable the console menu (local development / CLI mode).
console.menu.enabled=false

# Allow larger ARFF/CSV uploads
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=55MB
